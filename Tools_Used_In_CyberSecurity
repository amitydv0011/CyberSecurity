Information Gathering Tools in Cybersecurity
1. Network Scanning & Enumeration

Nmap → Scans open ports, services, versions.

Zenmap → GUI version of Nmap.

Masscan → Ultra-fast port scanner.

Unicornscan → Asynchronous port scanning.
ghost_eye -> use information gathering.

Netcat (nc) → Banner grabbing, port scanning, reverse shell.

2. DNS & Domain Information

Whois → Domain ownership details.

Nslookup / Dig → DNS records lookup.

DNSenum → DNS enumeration.

Fierce → Domain reconnaissance tool.

Sublist3r → Subdomain enumeration.

3. Web Information Gathering

WhatWeb → Identifies websites, frameworks, and technologies.

4. Brute Force & Dictionary Attacks
Hydra → Brute-force login tool (works on many protocols).
Crunch → Creates custom password lists for Wi-Fi cracking.


5.Specialized Wi-Fi Tools
Reaver → Exploits WPS (Wi-Fi Protected Setup) to crack WPA/WPA2.
Fern Wi-Fi Cracker → GUI-based wireless auditing tool.
Cowpatty → WPA-PSK brute force tool.
Wappalyzer → Browser extension for tech fingerprinting.

Dirb / Gobuster → Directory brute forcing.

Burp Suite (Community/Pro) → Web vulnerability scanner & proxy.

Nikto → Web server scanning for vulnerabilities.

4. Email & People Search (OSINT)

theHarvester → Gathers emails, subdomains, hosts, etc. from public sources.

Maltego → Relationship mapping (people, companies, domains).

Recon-ng → Powerful OSINT framework.
Hunter.io → Finds email addresses from domains.



Nmaps commands:-
🔹 1. Scan a single host
nmap 192.168.1.1

🔹 2. Scan multiple hosts
nmap 192.168.1.1 192.168.1.2

🔹 3. Scan a range of IPs
nmap 192.168.1.1-20

🔹 4. Scan an entire network
nmap 192.168.1.0/24

🔹 5. Scan specific port
nmap -p 80 192.168.1.1

🔹 6. Scan range of ports
nmap -p 1-1000 192.168.1.1

🔹 7. Service version detection
nmap -sV 192.168.1.1

🔹 8. Detect operating system
nmap -O 192.168.1.1

🔹 9. Aggressive scan (OS + services + traceroute)
nmap -A 192.168.1.1

🔹 10. Stealth (SYN) scan
sudo nmap -sS 192.168.1.1


Popular SQL Injection Tools
1. sqlmap (Most Popular)

An open-source penetration testing tool that automates SQL Injection detection and exploitation.

Supports union, error-based, boolean-based, time-based, and out-of-band SQLi.

Can enumerate databases, dump tables, crack hashes, and even get a remote shell.

Example command:

sqlmap -u "http://testphp.vulnweb.com/listproducts.php?cat=1" --dbs


 Finds if the parameter cat is injectable and lists databases.

2. Havij (GUI Based – Windows)

Old but famous automated SQLi tool with a graphical interface.

Can extract database info, tables, users, and even write files on the server.

Easy for beginners, but less used now (detected by most security systems).

3. jSQL Injection (Java Based)

Open-source GUI tool.

Supports multiple databases (MySQL, Oracle, PostgreSQL, MSSQL, etc.).

Can dump data, read files, write shell, etc.

4. NoSQLMap

Used for testing NoSQL injection (MongoDB, CouchDB).

Similar to sqlmap but for NoSQL databases.

5. BBQSQL (Python Library)

A blind SQL injection exploitation framework.

Useful when you can’t directly see query results (blind SQLi).

6. SQLNinja

Targets MSSQL (Microsoft SQL Server).

Exploits SQLi to gain DB access and potentially system access.

7. Exploit Frameworks with SQLi Modules

Metasploit Framework → has auxiliary modules for SQLi testing.

Burp Suite (Pro) → scanner can detect SQL injection automatically
